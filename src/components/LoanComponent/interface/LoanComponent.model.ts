/**
 * This interface is used to type the props for the `LoanComponent` component
 * > **edit** : this is an index for the loan to be updated in the `LoanContext` context.
 */
export interface ILoanComponentProps{
    edit?: number;
}

